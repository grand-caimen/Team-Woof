var app = angular.module('cityQuestApp',[
                         'cityQuest.services',
                         'cityQuest.city',
                         'cityQuest.questList',
                         'cityQuest.questView',
                         'cityQuest.createQuest']);

app.config(function(){});

app.run(function($rootScope){});
